# Frontend environment variables
# Copy to .env (dev) or .env.prod (prod) and fill in values

# URL of the backend API — must be reachable from the user's browser
# Dev:  PUBLIC_API_URL=http://localhost:8000
# Prod: PUBLIC_API_URL=https://api.yourdomain.com
PUBLIC_API_URL=http://localhost:8000
